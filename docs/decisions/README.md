# 关键决策记录

> 按时间线整理的架构决策记录（ADR）——记录当前架构、设计决策、关键修正
> 及其动机。每条决策带手动短锚点 `<a id="dN"></a>`，可被其它文档以
> `decisions/<区间>.md#d<编号>` 形式直接链接。

## 分卷目录

- [01-10.md](01-10.md)
- [11-20.md](11-20.md)
- [21-30.md](21-30.md)
- [31-40.md](31-40.md)
- [41-50.md](41-50.md)
- [51-60.md](51-60.md)
- [61-70.md](61-70.md)
- [71-80.md](71-80.md)
- [81-90.md](81-90.md)
- [91-100.md](91-100.md)
- [101-110.md](101-110.md)
- [111-120.md](111-120.md)
- [121-130.md](121-130.md)

---

## 纯计数目录

1. 节点声明改为 TestModule 式构造注入
2. 采样帧率独立为「抽帧」节点（2026-08 调整）
3. 截取节点拆分
4. 清单携带预览图
5. 裁剪规格合成
6. NodeGraphQt 显示修复
7. 端口类型校验与自动模式调整
8. 截取百分比化与面板增强
9. GIF 后端 Wand 化
10. 视频流式解码
11. 节点执行进度
12. 移除 Wand 进度监视器（关键修复）
13. GIF 解包 alpha 修复
14. GIF 元数据快速探测
15. 截取参数改起点% + 持续时长
16. 重跑清理旧缓存
17. 仿色节点（序列→序列）+ GIF 合成节点精简 + GIF 元数据增强
18. 仿色节点恢复「颜色数」参数 + 「分析」类别分析节点
19. 分析节点细化（用户反馈）
20. 1:1 查看预览框：完全按素材原始尺寸显示（不缩放）
21. 「停止」按钮（立即终止运行节点）
22. 右侧面板 QGroupBox 分割
23. hotkeys 由 JSON 改为代码实现
24. 顶栏视图/克隆操作（用户需求）
25. 节点说明面板悬停预览（用户需求）
26. 自动模式：数值输入组件编辑期间不运行（用户需求）
27. 顶栏撤销/重做与常规快捷键（用户需求）
28. 截取串行合成（compose_trim）
29. loguru 软件日志（用户需求）
30. 临时缓存目录迁移（用户需求）
31. 背景框（BackdropNode）
32. 抽帧节点帧滑条预览（用户需求）
33. 截取串行合成窗口钳制（用户需求）
34. 底部状态栏继承重写（用户需求）
35. 格式化 PNG 输出改为点击导出弹窗（用户需求）
36. 序列图片产物不再携带帧率/帧速信息（用户需求）
37. 仿色节点扩展为「颜色深度」节点（PS 式）
38. 「调色板先行」修正（已回退，2026-08）
39. 「取色方式」参数（已回退，2026-08）
40. 序列截取节点 + 三个一般处理节点 + 单图输入 + GIF 合成 1:1 预览 + 颜色深度互斥置灰
41. 运行键 qtawesome 化 + 运行键可中断 + 状态栏保持原消息
42. GIF 合成优化拆分 + 「序列处理」分类 + 设置管理器
43. 序列逐帧 PNG 导出并行化
44. 缓存 PNG 不压缩（速度优先，用户决策）
45. GIF 解包去掉双重编码（原始 RGBA 字节直取）
46. 序列相加 + 序列倒带 + RGBA 通道分离 + 1:1 查看滑条化
47. 多输出端口执行计划
48. GIF 预览透明修复（Qt QMovie 忽略透明色索引）
49. 画面裁剪可视化（红线拖拽）
50. RGBA 通道合并 + 1:1 查看透明背景 + 自动模式报错不弹窗
51. 唯一定义源头重构（options.py + 构造期一致性校验）
52. 反相节点 + 运存缓存可行性评估
53. 缓存管理增强：可调缓存路径 + 总大小限制（用户需求）
54. 分辨率统一 + 序列长度统一 + A通道分离/合并（用户需求）
55. 序列长度统一延长 + A通道提取改名 + 端口名显示 + GIF 合成默认值（用户实测）
56. 颜色深度 octree 量化破坏 alpha 修复（灰度内容/灰度色彩空间）
57. 三处修复：1:1 查看「串台」/ 棋盘格透明背景 / 进度标签不显示
58. 设置对话框显示缓存实时用量
59. 节点方案存档剥离纯 UI 字段
60. faulthandler 转储时间戳 + 不稳定状态栏警告
61. 空转约 10 分钟后 0xC0000005 崩溃排查
62. 崩溃转储定位与 faulthandler 日志轮转
63. 右键菜单完整移植 + 空闲 CPU 优化（用户需求）
64. 单 action 管理器重构 + 菜单瘦身 + 通常保存（用户需求）
65. 动作定义收敛为单一登记处（actions.py）（用户需求）
66. 主窗口菜单栏（QMenuBar + 多级菜单）（用户需求）
67. 背景框标题就地重命名 + 定义收归 create_backdrop（用户需求）
68. 源码按范畴分子包重构
69. 新增旋转纵横比挤压空白序列节点与通道处理分类（用户需求）
70. 旋转节点「裁剪成品」改为纯数学外接矩形裁剪（用户实测修正）
71. 旋转节点重写：内接最大矩形裁剪（用户需求）
72. 存档保留节点尺寸相关
73. 新增「超级键」与「ico合成」节点（用户需求）
74. gif/ico 专用分析节点 + 输出合成节点清单端口（用户需求）
75. 帧优化透明丢失修复 + ico合成改输出类节点（用户需求）
76. 颜色深度节点过时 + 新增颜色量化节点 + gifsicle 调研存档（用户需求）
77. GIF 合成节点回归原样合成（用户需求）
78. GIF 优化节点（gifsicle 后处理）（用户需求）
79. 渐变序列节点
80. 旧存档兼容读取（用户需求）
81. 撤销/重做记录节点内参数变化
82. 大文件按职责细分（nodes/media/ui 三处）
83. app_root_dir Nuitka 打包路径修复（`__compiled__` 探测）
84. 可写数据迁出程序目录（用户数据目录 user_data_dir）
85. 方案未保存（dirty）判定：撤销栈派生 + 空画布恒干净（用户需求）
86. 预览框高 DPI 像素校正（CheckerPreviewLabel 自绘；内容设备像素精准、框跟随图片、跨屏重建）
87. 平移滚动节点（动效处理分类）（用户需求）
88. 启动画面（QSplashScreen + logo.gif 动画）+ 运行时硬校验（用户需求）
89. 菜单栏「导入预设」快捷入口：node_presets 目录增量导入（用户需求）
90. 设置管理器固定深色主题，不再可调整（用户需求）
91. 设置对话框关于页重做：滚动 + img_resource.qrc 图标（用户需求）
92. Nuitka attach 双击启动：gifsicle subprocess 抛 WinError 6（stdin=DEVNULL 修复）
93. 控制台子进程黑框闪现（CREATE_NO_WINDOW）
94. 序列叠加节点 + 缩放策略「不缩放」+ 双输入序列节点端口位调换（用户需求）
95. 画面裁剪改为序列级节点 + 1:1 交互预览（用户需求）
96. 帧差静止保持节点（序列处理）（用户需求）
97. GIF 合成跳过 ≤256 色二次量化（用户需求）
98. MainWindow mixin 拆分回滚为单类（组合替代继承）
99. MediaBackend mixin 拆分回滚为单类（决策 #98 的配套）
100. GIF 合成(FFmpeg)节点：PyAV 进程内 palettegen/paletteuse 管线（用户需求）
101. WebP/APNG 动画导出节点：Pillow 内建（用户需求）
102. 输出类节点元数据仅关键信息 + gif优化分析去帧明细 + gif调色板固定 16×16（用户需求）
103. 启动画面构造期事件泵动（MainWindow 构建不再冻结动画）（用户实测）
104. gif优化分析 增加「帧优化占比」+ 帧优化统计改按文件结构（用户需求）
105. 新增「帧冻结」节点（序列 → 序列）（用户需求）
106. 序列相加同分辨率直接复制（快路径）（用户需求）
107. 序列剃刀节点（序列 → 两个序列，PR 剃刀工具式）（用户需求）
108. 智能布局：分层（Sugiyama 式）紧凑方形排布（已回退，2026-08）
109. 接管型参数 + PanelSpec：ParameterPanel 不再做加法（技术债清理）
110. 分类色功能图标系统：Bforartists 式「着色底+白形」+ 标题栏/节点库集成（用户需求）
111. 重做 #110：qtawesome 叠加图标（自绘 SVG → 现成图标库多层堆叠）（用户需求）
112. 节点 execute 显式化：拆 ColorAdjustmentNode / InputNode 族基类 + 类 docstring 自述约定（用户需求）
113. 背景框增强：可调标题栏高度 + 大字体标题背景纹理 + 节点列表图标显式化（用户需求）
114. 颜色参数化：Material 2014 查表 + 主题令牌收编（用户需求）
115. 序列截取升级为胶片条双手柄拖拽（与序列剃刀同构）（用户需求）
116. 新增「画面翻转」节点（序列 → 序列）（用户需求）
117. 界面配色重新设计：节点图壳色（方案 A）+ OKLCH 等距环分类色（方案 B-1）（用户需求）
118. 方案 C：连线/端口色统一 + 网格换挡修复 + 背景框配色收编（用户需求）
119. 方案 C 例外补全：克隆节点后连线色回退库默认橙（决策 #118 修正）
120. MediaBackend 按职责区段拆分为七个纯函数模块（薄转发保留 API）（缓解巨型单体问题）
121. GIF 合成时长一致性（累积舍入）+ gif优化分析 自有缓存副本与时长诊断元数据 + 删除/重跑释放下游预览（用户需求）
122. 新增 ico输入 节点（ICO 容器解码为从大到小 PNG）+ 修复 GIF 调色板探测 O(n²) 卡顿（用户需求）
123. 复杂色表（gifski 式每帧局部色表）支持修复：并集不抛错 + 色表口径准确（用户需求）
124. 新增「GIF 合成(gifski)」节点：gifski CLI 随包引入（每帧独立调色板，用户需求）
125. GIF 合成(gifski) 修复：「文件名或扩展名太长」+ 颜色选择器置灰（用户实测）
126. 调色板类型元数据（gif输入 / gif优化分析）+ 调色板查看逐帧 slider（用户需求）
127. 关于页新增 gifski 后端小节（用户需求）
