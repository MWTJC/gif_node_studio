# 节点清单（49 个，`NODE_CLASSES` 顺序）

> 节点库除下列 48 个节点外，另有「背景」分组的**背景框**按钮（NodeGraphQt
> `BackdropNode`，`kind="backdrop"`）——它不是 StudioNode，不进 `NODE_CLASSES`，
> 无端口/参数/执行，仅作画布分组框。

| kind | 类 | 类别 | 输入→输出 | 说明 |
|---|---|---|---|---|
| `video_input` | `VideoInputNode` | 输入 | → 格式化清单 | 首帧预览；元数据含总时长/帧率 |
| `image_sequence_input` | `ImageSequenceInputNode` | 输入 | → 格式化清单 | 连续编号序列发现；也允许单张非序列图片（作为单帧序列） |
| `gif_input` | `GifInputNode` | 输入 | → 格式化清单 | 首帧预览；自身预览框直接播放 gif |
| `blank_sequence` | `BlankSequenceNode` | 输入 | → 序列图片 | 空白序列：无输入，输出不透明纯白序列；可定义分辨率（宽×高）与帧数（均为整数）；供「序列相加」「分辨率统一」等多输入节点作对齐基准/背景（配合「分辨率统一」最近邻缩放可实现像素画风倍数放大）；帧滑条预览 |
| `gradient_sequence` | `GradientSequenceNode` | 输入 | → 序列图片 | 渐变序列：无输入，输出线性渐变 RGB 序列；可定义分辨率（宽×高）、帧数、起点/终点颜色（色块按钮选取）与渐变角度（-180°..+180°）；供「序列相加」「分辨率统一」等多输入节点作对齐基准/背景，或直接作为渐变蒙版素材；帧滑条预览 |
| `time_trim` | `TimeTrimNode` | 预格式化 | 清单→清单 | 仅视频；起点 % slider + 持续秒数；起点帧预览；同模式合成 |
| `frame_trim` | `FrameTrimNode` | 预格式化 | 清单→清单 | 起点 % slider + 持续帧数（int）；起点帧预览；同模式合成 |
| `format` | `FormatNode` | 格式化 | 清单→序列 | 分辨率%；产物为图片序列（不含帧率信息）；帧滑动预览 |
| `brightness` | `BrightnessNode` | 一般处理 | 序列→序列 | 亮度 |
| `saturation` | `SaturationNode` | 一般处理 | 序列→序列 | 饱和度 |
| `hue` | `HueNode` | 一般处理 | 序列→序列 | 色相 |
| `color_balance` | `ColorBalanceNode` | 一般处理 | 序列→序列 | RGB 通道缩放 |
| `binarize` | `BinarizeNode` | 一般处理 | 序列→序列 | 二值化：阈值滑条 0–255，转灰度后按阈值输出纯黑/纯白（Alpha 保留）；帧滑条预览 |
| `grayscale` | `GrayscaleNode` | 一般处理 | 序列→序列 | 灰度化（R==G==B，Alpha 保留）；帧滑条预览 |
| `contrast` | `ContrastNode` | 一般处理 | 序列→序列 | 对比度调整（-100..100，Alpha 保留）；帧滑条预览 |
| `invert` | `InvertNode` | 一般处理 | 序列→序列 | 反相（RGB 各通道取 255−原值，如黑↔白、红↔青，Alpha 保留）；帧滑条预览 |
| `super_key` | `SuperKeyNode` | 一般处理 | 序列→序列 | 超级键（PR 颜色键/超级键式抠像）：背景色由 QColorDialog 选取（色块按钮 + 十六进制显示，默认绿幕 #00ff00），颜色接近背景色的像素变为透明；边缘处理强弱滑条 0–100%（0% 硬边二值抠像、100% 大范围柔和羽化过渡）；Alpha 只减不增；帧滑条预览 |
| `rotate` | `RotateNode` | 一般处理 | 序列→序列 | 旋转：滑条 -180°..+180° 旋转每一帧（正角度逆时针），空隙用全透明像素填充；不勾选「内接最大矩形裁剪」（默认）时按最小外接矩形确定画布边界；勾选后裁剪为旋转后内容的最大内接矩形（画布更小、无空隙）；帧滑条预览 |
| `aspect_squeeze` | `AspectRatioNode` | 一般处理 | 序列→序列 | 纵横比挤压：滑条 0.2..5.0（1.0=不变）非等比缩放帧宽（高度不变）控制纵横比——<1 横向压扁（更窄）、>1 横向拉宽（更扁），即新纵横比=原纵横比×因子；帧滑条预览 |
| `flip` | `FlipNode` | 一般处理 | 序列→序列 | 画面翻转：按所选方向翻转每一帧——水平=左右镜像、垂直=上下翻转；几何变换逐像素搬运，Alpha 随像素正确翻转，输出尺寸不变；帧滑条预览 |
| `sequence_crop` | `SequenceCropNode` | 一般处理 | 序列→序列 | 画面裁剪（替代旧清单级 `crop`，旧存档读取时跳过）：直接裁剪已格式化序列，可放在处理链任意位置；**1:1 可视化裁剪**——预览框 = 素材像素尺寸（跟随图片，DPI 感知），在原始像素上拖拽四条红线（结果缩略图实时预览，拖拽折叠为一条撤销）；纵横比下拉锁定裁剪框比例（自由/1:1/4:3/3:2/16:9/21:9/9:16，锁定时拖边/拖角/平移均维持比例，切换比例按中心与面积重投影） |
| `ping_pong` | `PingPongNode` | 序列处理 | 序列→序列 | 序列倒带（只倒序输出，不再自行追加；如需往复再接「序列相加」）；帧滑条预览 |
| `sequence_add` | `SequenceAddNode` | 序列处理 | 序列B(待处理)/序列A(基准)→序列 | 序列相加：以 A 分辨率为基准缩放 B 后追加（端口位约定：待处理 B 在上、基准 A 在下，决策 #94）；缩放算法（最近邻/双线性/双三次/兰索斯）+ 缩放策略（拉伸/填充/适合/不缩放，默认适合，未填满区域全透明黑；不缩放=保持 B 原尺寸，单轴超出画布时该轴居中裁剪）；**快路径：B 分辨率与 A 相同时直接复制不进行缩放**（缩放/合成均为恒等变换，与 A 帧同路径，提高效率）；帧滑条预览 |
| `sequence_overlay` | `SequenceOverlayNode` | 序列处理 | 序列A(基础)/序列B(叠加)→序列 | 序列叠加：把 B 逐帧**层叠**到 A 上（平面叠加，输出第 i 帧 = A[i] 与按所选缩放算法/策略处理的 B[i] 合成，B 的透明通道参与混合；A 决定画布与输出长度）；输出长度 = A 长度——B 帧不足时**直接循环重复**、B 长于 A 时取前 len(A) 帧；缩放算法/策略与「序列相加」一致（含「不缩放」= 保持 B 原尺寸 1:1 居中叠放，单轴超出画布居中裁剪）；帧滑条预览 |
| `sequence_trim` | `SequenceTrimNode` | 序列处理 | 序列→序列 | 序列截取：**胶片条上拖动起止双手柄**选择区间（蓝色起始手柄/橙色结束手柄，PR 时间轴式，决策 #115）仅输出 [起始帧, 结束帧) 区间内的帧——区间内部高亮、外部压暗，拖拽**实时预览区间首尾帧**（起始帧/结束帧，不依赖运行）；起始手柄可拖到最左（从头截取）、结束手柄可拖到最右（截取到末尾），区间恒非空；「起始帧/结束帧」参数（int）由胶片条接管（不显示数值行）；数值防呆（区间非法/越界报错）；参数名/值域与旧版一致，旧存档读取透明 |
| `sequence_razor` | `SequenceRazorNode` | 序列处理 | 序列→段A/段B 两个序列 | 序列剃刀（PR 剃刀工具式，用户需求）：**胶片条上拖拽红色剃刀线**把序列切成两段——段A = 切割点之前（第 1..cut 帧）、段B = 切割点之后（第 cut+1..末尾 帧），两端非空；切割位置吸附帧边界，拖拽时**实时预览切割处两侧帧**（段A末帧 / 段B首帧，不依赖运行）；「切割帧」参数（int）由剃刀条接管（不显示数值行）；多输出端口按端口名解析 |
| `resolution_align` | `ResolutionAlignNode` | 序列处理 | 序列B(待处理)/序列A(基准)→序列B | 分辨率统一：以 A 分辨率为基准缩放 B（缩放算法/策略与「序列相加」一致；帧数不变）；供通道合并前对齐各通道分辨率；帧滑条预览 |
| `length_align` | `LengthAlignNode` | 序列处理 | 序列B(待处理)/序列A(基准)→序列B | 序列长度统一：按 A 长度统一 B（B 短于 A 时按方式延长：循环复制按周期重复 [abc]→[abcab]，均匀采样重复帧均匀分布 [abcd]→[aabbccdd]、非整数倍如→[abbcdd]；B 长于 A 时截取前 len(A) 帧）；分辨率不变；供通道合并前对齐各通道长度；帧滑条预览 |
| `ico_compose` | `IconComposeNode` | 输出 | 序列1..序列6→序列+清单 | ico合成（输出类，含「导出…」按钮）：把若干单帧序列合成为多尺寸图标序列；每个输入序列必须为单帧（长度 ≠ 1 报清晰中文错误）；「自动分级」勾选（默认）取分辨率最高的输入，按常见 icon 分辨率阶梯（16/24/32/48/64/128/256/512/1024）逐级缩小（只缩小不放大，最小保证 16×16，源小于 16×16 报错；非方形源按「适合」等比缩放居中贴方形透明画布不拉伸）；不勾选按端口顺序原样输出各输入单帧（手动提供各尺寸）；输出序列每帧=一个尺寸等级（升序）；运行写缓存 preview.ico（<256 用 32bpp BMP DIB、≥256 用 PNG 的 Vista 式容器），「导出…」弹框保存（默认 output.ico）；帧滑条预览；另输出「格式化清单」端口（各分辨率帧），供 ico分辨率查看 使用 |
| `gif_export` | `GifExportNode` | 输出 | 序列→序列+清单 | 帧速/尺寸%（按输入**原样合成**：不做帧优化/透明优化，帧间未变化区域全幅存储；颜色数/仿色由上游「颜色量化」节点控制）；「透明预览」勾选（纯显示，绿幕/品红/棋盘格背景，便于观察透明通道）；运行写缓存 preview.gif，「导出…」弹框保存（默认 output.gif）；预览框 1:1（与图片1:1分辨率查看一致，按 GIF 原始尺寸显示）；另输出「格式化清单」端口（生成的 gif 文件），供 gif优化分析 节点查看存储情况 |
| `gif_export_ffmpeg` | `GifExportFfmpegNode` | 输出 | 序列→序列+清单 | GIF 合成(FFmpeg)：PyAV 进程内 palettegen/paletteuse 业界事实标准管线（零新依赖，决策 #100）——palettegen 对整段序列生成**共享调色板**（与「颜色量化」MagickQuantizeImages 语义一致防帧间闪烁）+ paletteuse 映射（仿色）+ gif 编码器；颜色数（max_colors 2–256）/调色板统计（整段 full 默认/帧间差异 diff/单帧 single）/仿色（Floyd-Steinberg 默认/none/Atkinson/Sierra2-4A/Bayer 有序/Heckbert，Bayer 粒度 0–5 仅 Bayer 生效）/帧优化（默认开 = diff_mode=rectangle，**编码时直接只存变化区域**，gifsicle -O2 级无需后处理，开启后未变化区域以透明索引存储，可用 gif优化分析 存储帧视图目检）；⚠️ FS 系误差扩散对相同帧输出不同图案（录屏冻结建议配「帧差静止保持」+无仿色或 Bayer）；透明输入按 GIF 1-bit 语义保留（半透明二值化）；「透明预览」纯显示；运行写缓存 preview.gif，「导出…」弹框保存（默认 output.gif）；预览框 1:1；另输出「格式化清单」端口 |
| `gif_optimize` | `GifOptimizeNode` | 输出 | 清单→清单 | GIF 优化节点（gifsicle 后处理）：对 GIF 合成输出的 GIF 文件做**文件级优化**——优化级别（-O1 只存变化区域/-O2 另做透明优化/-O3 多策略择优，默认）+ 有损度（--lossy 0–200，0=关闭，数值越大体积越小伪影越明显）+ GIF 级降色（--colors+--color-method：diversity 严格子集/blend-diversity 混合/median-cut 中位切割，配合仿色 --dither：Floyd-Steinberg/Atkinson/ro64/o3/o4/o8/有序，与固定色板 --use-colormap：web 216 色/灰度/黑白/自定义文本色板或 GIF 色表）+ 兼容模式（--careful）；不保证缩小（罕见变大），--lossy/--colors 不可逆重复优化累积伪影；运行写缓存 preview.gif（优化后），「导出…」弹框保存；预览框 1:1；输出「格式化清单」端口（优化后的 gif），供 gif优化分析 目检优化效果（存储帧视图） |
| `dither` | `DitherNode` | 一般处理 | 序列→序列 | ⚠️ **已过时**：新工作流请用「颜色量化」节点。颜色深度节点（PS「存储为 Web 所用格式」式）——降低颜色深度算法（可感知(自适应)/随样性(感知)/受限(Web)/灰度/黑白/Windows/Mac OS）+ 仿色算法（扩散/无仿色/图案/杂色）+ 颜色数 + 图案阈值图/均匀色阶；互斥控件置灰（颜色数仅可感知/随样性/灰度可用，阈值图/均匀色阶仅图案时可用）；黑白输出只有纯黑/纯白；Windows/Mac OS 走 data/palettes/ 系统色板 PNG（可替换）；帧滑条预览。 | 
| `color_quantize` | `ColorQuantizeNode` | 一般处理 | 序列→序列 | 颜色量化节点（IM 原生）：量化色彩空间（-quantize：sRGB/RGB/灰度/透明(含Alpha)/Lab/Luv/LCH/HCL/YCbCr/HSB/HSV/HSL/HWB/CMYK/XYZ/Log）+ 颜色数（-colors 2–256，整条序列共享调色板）+ 树深度（-treedepth 0=自动）+ 仿色（-dither：扩散(Floyd-Steinberg)/Riemersma/无仿色）+ 有序仿色（-ordered-dither：阈值图+均匀色阶，勾选后仿色置灰）+ 海报化色阶（-posterize 每通道均匀色阶，0=关闭）；灰度空间=先转灰度再量化（等价 -colorspace gray -colors N）；透明空间=Alpha 纳入量化（GIF 1-bit 透明语义），其余空间保留原 Alpha 精确不变；帧滑条预览 |
| `sampling` | `SamplingNode` | 序列处理 | 序列→序列 | 抽帧：定义帧速/输出帧速（int，纯数值框，由用户输入）按比值抽帧（间隔=定义/输出，目标帧驱动，如 30→12 取源 0、2、5、8…）；输出 ≥ 定义时不抽帧；帧滑条预览 |
| `static_hold` | `StaticHoldNode` | 序列处理 | 序列→序列 | 帧差静止保持：逐帧与参考帧逐像素比较，差值 ≤ 静止阈值判定为「静止」的像素**沿用参考帧的精确值**（携带保持后的版本，噪声不回流），运动区域保留当前帧——静止区域因此逐帧像素精确一致。针对「电脑录屏 → GIF」：消除录屏编码（H.264/HEVC）在静止 UI 上的时域量化噪声，量化取同一色板项（无帧间闪烁）、gifsicle 帧优化可合并静止区域（体积下降）。参数：静止阈值（0–255，逐通道最大允许差值，录屏噪声通常 1–3，需 ≥ 噪声幅度才能全冻结）；参考帧（前一帧=流式因果、内容变化后自动恢复 / 首帧=静止背景统一到首帧、内容变化后该区域永久视为运动）；邻域判定（0–8，默认 4，沿用静止值还需 ≥N 个 8 邻域像素也静止，剔除运动边缘孤立静止像素防拖尾）；Alpha 原样保留；帧滑条预览。⚠️ 量化侧注意（决策 #96/#97）：IM Floyd-Steinberg 仿色对相同帧也输出不同图案（误差扩散变化敏感）——本节点工作流建议配「颜色量化」的**无仿色**（或 Riemersma）；「GIF 合成」已在序列 ≤256 色且未缩放时跳过二次量化（决策 #97），不再破坏冻结 |
| `frame_freeze` | `FrameFreezeNode` | 序列处理 | 序列→序列 | 帧冻结：把首帧/最后一帧的静态内容**定格**延长若干帧——冻结位置（第一帧=在序列开头插入 K 份首帧副本，首帧定格在开头；最后一帧=在序列末尾追加 K 份末帧副本，末帧定格在结尾）+ 冻结延长帧数（int，0=原样输出）；冻结帧为边界帧的逐像素副本（直接复制，无插值/过渡），常用于 GIF 片头定格/结尾停留；帧滑条预览 |
| `pan_scroll` | `PanScrollNode` | 动效处理 | 序列→序列 | 平移滚动：画面向所选方向（上/下/左/右）**无缝循环平移**（跑马灯式，被推出画布一侧的像素从对侧绕回，不产生黑边/接缝）；输出帧数 = 持续帧数（int），输入序列不足时按周期循环补足、不少于时取前 N 帧；速度曲线下拉（匀速/线性加速 p²/线性减速 1−(1−p)²/win10减速 = Win10 动效官方减速曲线 cubic-bezier(0.1,0.9,0.2,1)，起步快减速至停），整段动画恰好走满一个画面循环（宽/高）、首尾无缝衔接成循环；插值方式下拉（双线性平滑/最近邻像素锐利，供像素画素材自行选择）；帧滑条预览 |
| `channel_split` | `ChannelSplitNode` | 通道处理 | 序列→红/绿/蓝/透明度四个序列 | RGBA 通道分离：四通道均以灰度图输出（R==G==B、不透明），透明度通道灰度值=原 alpha；多输出端口按端口名解析；帧滑条预览（首个通道） |
| `channel_merge` | `ChannelMergeNode` | 通道处理 | 红/绿/蓝/透明度四序列→序列 | RGBA 通道合并（与通道分离互逆）：每路输入取灰度值作为对应通道值，透明度通道未连接按不透明处理（alpha=255）；各通道长度/尺寸不一致、必填通道缺失或为空均报清晰中文错误；帧滑条预览 |
| `alpha_split` | `AlphaSplitNode` | 通道处理 | 序列→透明度通道序列 | A通道提取：提取 alpha 通道为灰度序列（灰度值=原 alpha，与 RGBA 分离的透明度通道语义一致），但只物化 1 份缓存（RGBA 分离会同时物化红/绿/蓝/透明度 4 份）；帧滑条预览 |
| `alpha_merge` | `AlphaMergeNode` | 通道处理 | RGB序列/透明度通道→序列 | A通道合并（与 A通道提取 互逆）：RGB 序列保留彩色通道 + 透明度通道灰度值合成 RGBA；透明度通道长度自动对齐（短延长/长截取）、未连接按不透明处理；帧尺寸不一致报清晰中文错误；帧滑条预览 |
| `png_export` | `PngExportNode` | 输出 | 序列→ | 运行写缓存 preview_frames/；「导出…」弹框保存（默认前缀 sequence_，命名 sequence_0001.png…） |
| `webp_export` | `WebpExportNode` | 输出 | 序列→序列 | WebP 动画导出（Pillow 内建，零新依赖，决策 #101）：RGBA 透明保留、体积通常远小于 GIF；质量 0–100/无损编码勾选；⚠️ 序列含透明时自动无损（Pillow WebP 动画有损路径丢 alpha，Pillow #8101 同类）；运行写缓存 preview.webp，「导出…」弹框保存（默认 output.webp）；预览首帧 1:1；输出「序列图片」透传端口 |
| `apng_export` | `ApngExportNode` | 输出 | 序列→序列 | APNG 导出（Pillow 内建，零新依赖，决策 #101）：无损格式（PNG 帧 + acTL 动画块）alpha 全保留；运行写缓存 preview.apng，「导出…」弹框保存（默认 output.apng）；预览首帧 1:1；输出「序列图片」透传端口 |
| `palette_view` | `PaletteViewNode` | 分析 | 清单/序列→（无输出） | gif调色板查看：GIF 读颜色表条目（与元数据一致）、序列统计像素；>256 色报错不支持；禁视频；色板图为**固定 16×16 色块**（RGBA，缺色格透明），预览框 1:1 |
| `res1to1_view` | `ResolutionViewNode` | 分析 | 清单/序列→（无输出） | 图片1:1分辨率查看：完全按素材原始尺寸显示（不缩放不裁剪）；序列可滑条逐帧查看，GIF 直接播放动画（透明 GIF 走自定义播放器），框 = 素材像素尺寸；「透明背景」勾选后预览框改用绿幕/品红色或 PS 式棋盘格（设置中可选，默认绿幕）便于观察透明通道 |
| `gif_analysis_view` | `GifAnalysisNode` | 分析 | 清单→（无输出） | gif优化分析：特殊解码——按文件**实际存储**结构逐帧 1:1 查看（滑条逐帧），直观呈现帧优化（局部帧仅存变化区域，按偏移贴回画布、未存区域透明）与透明优化（帧间未变化像素置透明）的真实形态；「解码方式」下拉：存储帧（默认，按文件实际存储）/ 合成帧（coalesce 完整帧，与普通播放器一致，供对照）；「透明背景」纯显示勾选；元数据含帧优化/透明优化统计（**不含逐帧明细**，逐帧信息由滑条承担）与**帧优化占比**（以最短帧时间为基准：相对等时长全最短帧序列，优化后节省的帧数百分比，如「节省 42.9%（等时长全最短帧 7 帧 → 实际 4 帧）」）；帧优化统计**按文件结构计算**——合成帧模式同样如实报告局部帧数（coalesce 只抹掉显示层）；透明统计按帧自身存储数据计算 |
| `ico_analysis_view` | `IcoAnalysisNode` | 分析 | 清单→（无输出） | ico分辨率查看：把清单携带的各分辨率帧合成为 **1:1 拼贴图**（每帧按原始像素尺寸不缩放，从大到小打包成行，图标下方标注尺寸，透明区域透出棋盘格底纹），预览框 = 拼贴图尺寸；元数据列出分辨率数量/分辨率列表/最大分辨率/画布尺寸 |

端口约定：`格式化清单`（MANIFEST，橙色）与 `序列图片`（SEQUENCE，蓝色）。连线时按端口类型校验：不同类型端口拒绝连接并提示「非法连接：端口类型不符」；执行时同样校验，报错原因明确为端口类型不符。
